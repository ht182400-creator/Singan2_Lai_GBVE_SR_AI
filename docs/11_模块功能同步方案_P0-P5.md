# 11_模块功能同步方案（P0–P5）

> 目标：让 Web（`Singan2_Lai_GBVE_SR_AI/web`）的功能与原版 MFC（`Singan2_Lai_GBVE_SR_OLD`）逐模块对齐。
> 前置状态：UI 复刻约 90%（见 `Web_UI_1to1_视觉模型对照清单.md`），算法层 C++ 移植已对拍通过（M0/M1/M2，18/18）。
> 创建日期：2026-09-01

---

## 0. 结论速览

| 项 | 结论 |
|---|---|
| MFC 功能规模 | 主窗口 `WinMain.cpp` 约 50 个 `case IDC_*`；另含 20+ 国家算法、图像处理算子、ATB/VTB、图表、保存导出 |
| Web 当前功能 | **接近 0**——`web/src/api.js` 无任何 import，全站零网络请求；所有按钮只 `pushHistory()` 记日志 |
| 后端当前能力 | 仅 1 个：`run_algorithm` → `{s2[33], etc[15]}` |
| 最大瓶颈 | 不是算法（算法已移植且对拍通过），而是 **①后端 API 太薄 ②core 能力未暴露 ③Web 无数据层** |
| 执行顺序 | P0 基础数据链路 → P1 分析 → P2 图像处理 → P3 Graph → P4 ATB/VTB/坐标 → P5 保存与配置 |

---

## 1. MFC 原版功能结构（对齐基准）

### 1.1 分层与主流程

```text
UI 层      WinMain.cpp DialogProc
             ├─ WM_COMMAND   ~50 个 case IDC_*（按钮/菜单）
             ├─ WM_HSCROLL   滑块（Image Processing 参数）
             ├─ WM_DRAWITEM  图像绘制
             └─ WM_DROPFILES 文件拖放
              ↓
编排层      MainRun.cpp   MainRun / MainRun1 / MainRun2 / ReadImgDataNew / funDatas1-2
              ↓
分派层      ALL32.CPP     ALL32 → InitAll32 → 公共特征(C_SI2) → switch(国家) → 国家函数
              ↓
数据层      CTemplateData / CFileAccess / CDataHeader / CTemplateVTB
              ↓
算法层      C_SI2.CPP(32 维特征) / GRADIENT / PREWITT / LAPLACIAN / NITI /
           smooth_median / To2byte / bvmath(Rute 整数开方)
```

主流程：`读 .dat → ReadImgDataNew → MainRun → ALL32 → 国家函数 → S2[1..32]`

### 1.2 全局状态（`MAIN.H`）

| 变量 | 含义 | Web 对应 |
|---|---|---|
| `global_oneimg` / `global_twoimg` | 1 字节 / 2 字节图像缓冲 | 图像数据 |
| `global_Mai` | 当前记录号（枚） | `record` |
| `global_Zparam` | 坐标参数 | `zfile areas` |
| `S2[32]` | 32 维特征 | `s2` |
| `global_etc` | 补充特征 | `etc` |
| `global_SelectCountry` | 国家代码 | `country` |

图像规格：`Y_SIZE=88`、`X_SIZE=186`、单波段 16368 像素、13 波段/面。

### 1.3 主窗口控件→功能映射（`WinMain.cpp`）

| 分组 | 控件（`IDC_*`） | 功能 |
|---|---|---|
| 数据导航 | `NEXT` / `NEXT2` / `NEXT10` / `NEXT11` / `BACK` / `BACK2` / `BACK10` / `BACK11` | 记录前进/后退（步长不同） |
| 数据加载 | `BUTTON_DATA_GO1` / `BUTTON_DATA_GO2` | 载入数据（双通道） |
| 分析 | `RUN`、`IKKATU`（一括/批量）、`CREA` / `LISTCREA` | 执行分析、批量处理 |
| 图表 | `BUTTON_MAKE_GRAPH` / `MAKE_GRAPH_ALL` / `MAKE_COMBINE_GRAPH` / `BUTTON_SAVE_GRAPH` / `BUTTON_LOAD_GRAPH` / `BUTTON_CLEAR_GRAPH_LIST` | 生成/合成/存取/清空图表 |
| 图像处理 | `BUTTON_NITI_1` / `BUTTON_NITI_2`、`BUTTON_GR`、`PALETE`、`VER`、`K_DEFAULT` | 二值化、梯度、调色板、版本 |
| ATB | `BUTTON_LOAD_ATB` / `ATB_SET_4_DIR` / `ATB_CLEAR_4_DIR` / `ATB_SHOW_ALL` / `ATB_UPDATE` / `ATB_CLEAR` / `BUTTON_DISPLAY_ATB_SELECT` | ATB 模板载入与区域管理 |
| VTB | `BUTTON_LOAD_VTB`、`LIST_VTB`、`CHANGE_BUTTON` | VTB 特征模板 |
| 其它 | `BUTTON_LOAD_DENOS_SIZE`、`CUSTUM`、`M_SET`、`REN`、`RENKAKU`、`HOJI`（保存）、`ON_JYOKYO`、`WINDOWCHANGE`、`R_BREAK`、`KIN_COMBO`、`COMBO_ATB_TYPE` | 面额尺寸、自定义、保存、面额/ATB 类型下拉 |

对话框：`SProc.cpp`（波段 radio、MOUSE_KYOTYO、HEAD_ONOFF、Z_ON、Z_NAME_DRAW、ATB 全选/反选）、
`JProc.cpp`（`RADIO1_..RADIO11_` → 倍数 3..23）、`MFSETProc.cpp`（CHANGE_Z、R_OK、S2_COMBO、SL_CREA、ONE_DELETE、HE）、`SetManual.cpp`（OK）。

---

## 2. Web 现状与三大差距

### 2.1 现状

| 部分 | 状态 |
|---|---|
| `web/src/components/` 45 个组件 | UI 复刻约 90%，可拖拽容器（RC）已就绪 |
| `web/src/api.js` | 已定义 `health/analyzeByPath/analyzeUpload`，**但无任何组件 import** |
| 全站网络请求 | **0 处**（`fetch` 仅出现在 api.js 内部） |
| 按钮行为 | 一律 `pushHistory(msg)` 写 Operation History，无副作用 |
| 对话框 | 硬编码占位（如 Coordinate Settings 的 input/select 无行为） |
| Graph1 / Graph2 | 写死 `defaultValue` |
| `ListResultsView` | 用 `data/sample.js` 假数据 |
| `ImagePane` | 无真实图像数据 |

### 2.2 三大差距

1. **后端 API 太薄** —— 1 个能力 vs MFC 约 50 个操作。
2. **core 有能力没出口** —— `ImageEngine`（`to_2byte`/`compute_intermediate_waves`/`gradient`/`niti`/`smooth`/`rute`）、`mariner_reader`、`readzfile` 均已实现，server 未调用。
3. **Web 无数据层** —— 只有 UI 状态，缺 session / record / image / s2 数据模型与状态管理。

---

## 3. MFC 模块 → core 移植状态（决定各阶段可行性）

| MFC 模块 | core 对应 | 状态 | 影响阶段 |
|---|---|---|---|
| `CTemplateData` / `ReadImgDataNew` | `mariner_reader.cpp` | ✅ 已移植 | P0 |
| `ZAHYO_READ.CPP` | `readzfile.cpp` | ✅ 已移植 | P4 |
| `GBV_DIV_H.bin` 除法表 | `wtable.cpp` | ✅ 已移植 | — |
| `GRADIENT/NITI/smooth_median/To2byte/bvmath` | `imageops.cpp` | ✅ 已移植 | P2 |
| `C_SI2.CPP` | `c_si2.cpp` | ✅ 已移植 | P1 |
| `ALL32.CPP` + 各国算法 | `all32.cpp` | ✅ 已移植（主要国家） | P1 |
| `MainRun.cpp` 单枚编排 | `algo.cpp::run_algorithm` | ✅ 已移植（单枚） | P1 |
| `CreateGraph.cpp` / `OnDrawPaint.cpp` / `CGR_CLASS` | — | ❌ **未移植** | P3 阻塞 |
| `CTemplateVTB.cpp`（VTB 模板） | — | ❌ **未移植** | P4 阻塞 |
| ATB 区域管理逻辑 | — | ❌ **未移植** | P4 阻塞 |
| `save_load.cpp`（CSV/DB 导出） | — | ❌ **未移植** | P5 阻塞 |
| `singan2.si2` 配置读写 | — | ❌ **未移植** | P5 阻塞 |

> **重要**：P0–P2 可由现有 core 完整支撑；P3–P5 的"完全一致"需先补移植对应 MFC 模块。
> 因此 P3–P5 分两步：先用现有能力提供**可用实现**，再逐步补移植达到**逐项一致**。

---

## 4. 后端 API 设计（server 扩展目标）

统一前缀 `/api`，全部返回 JSON，带 CORS。

### P0 基础数据链路

| 端点 | 入参 | 返回 | 说明 |
|---|---|---|---|
| `POST /api/session/open` | `dat_path` | `{record_count, wave_count}` | 打开文件并统计记录数 |
| `POST /api/image` | `dat_path, record, wave, mode` | `{width, height, encoding, data(base64)}` | 取指定记录/波段图像 |
| `POST /api/small-image` | `dat_path, record` | `{data(base64)}` | 小图（SMALL_SIZE） |

### P1 分析链路

| 端点 | 入参 | 返回 |
|---|---|---|
| `POST /api/analyze-path` | `dat_path, zfile_path, record, kin, country` | `{s2[33], etc[15]}`（已有） |
| `POST /api/analyze` | multipart `dat` + 参数 | 同上（已有） |
| `GET /api/feature-names` | — | `{names:[32]}` 便于前端标注 |

### P2 图像处理

| 端点 | 入参 | 返回 |
|---|---|---|
| `POST /api/imageops` | `dat_path, record, wave, ops:[...]` | 处理后图像 |
| | `ops` 支持 `gradient{gtype,amp}` / `niti{s}` / `smooth` / `to2byte` / `intermediate` | |

### P3 Graph

| 端点 | 入参 | 返回 |
|---|---|---|
| `POST /api/graph/make` | `dat_path, record, zfile_path, kin, country, mode` | `{series, area, gap, stats}` |
| `POST /api/graph/combine` | `a, b, mode` | 合成序列 |
| `POST /api/graph/save` / `load` | `path[, series]` | 存取 `.grp` |

### P4 ATB / VTB / 坐标

| 端点 | 入参 | 返回 |
|---|---|---|
| `POST /api/zfile/parse` | `path` | `{areas:[{x1,y1,x2,y2,a_low,a_high,b_low,b_high,area_min}]}` |
| `POST /api/atb/load` / `update` / `clear` | `path` 等 | ATB 列表与操作 |
| `POST /api/vtb/load` | `path` | VTB 列表 |

### P5 保存与配置

| 端点 | 入参 | 返回 |
|---|---|---|
| `POST /api/export/csv` | `rows, path` | 导出结果 CSV |
| `POST /api/config/save` / `load` | `path[, config]` | `si2` 配置存取 |

---

## 5. 分阶段任务与验收

| 阶段 | 任务 | 依赖 | 验收标准 |
|---|---|---|---|
| **P0** | 数据链路：open / image / small-image；Web 建立 session 状态、真实图像渲染、记录导航 | core `mariner_reader` | 打开 `.dat` 能显示第 N 枚 IR1/IR2 真实图像；NEXT/BACK 切换记录图像随之变化 |
| **P1** | 分析链路：s2/etc 接入 Validation / Statistics / S2Chart / ResultDetails；国家、KIN、zfile 参数联动 | core `algo`/`all32`/`c_si2` | 点 RUN 后各面板显示后端真实 s2/etc，与原版对拍一致 |
| **P2** | 图像处理：gradient/niti/smooth/intermediate 作用于当前波段并重绘 | core `imageops` | 调参后图像实时变化，效果可比原版 |
| **P3** | Graph：生成/合成/存取图表序列 | 需补移植 `CreateGraph`/`OnDrawPaint` | 生成的序列与原版 `.grp` 可比 |
| **P4** | ATB/VTB/坐标：区域解析与模板管理 | `readzfile` ✅；ATB/VTB 需补移植 | 坐标文件解析正确；ATB/VTB 载入列表 |
| **P5** | 保存/CSV/配置：结果导出与配置持久化 | 需补移植 `save_load`/`si2` | 导出内容与原版一致；配置可存取 |

### 通用验收：对拍

所有涉及算法的改动，沿用 `06_改造套路与执行路线图.md` 第 7 节的对拍机制：
同一枚数据 → 原版 exe 输出 vs Web/后端输出，**逐项整型精确相等**；
差异按"解析偏移 / 数值语义 / 原版 BUG"分类登记。

### 测试数据

- `data/2A_DA_111017_115542.dat`（22MB / 40 枚）
- `data/ZAR/X_ATB_ZAR_132006050001.txt`（坐标，Shift-JIS）
- `GBV_DIV_H.bin`（除法表，仓库已有）

---

## 6. 执行约定

1. **算法不下放到 TS**：所有数值计算留在 C++ core，Web 只做展示与交互，避免引入数值偏差。
2. **新增能力先加 core，再加 server 端点，最后接 Web**：保持"能力→接口→界面"的单向依赖。
3. **每阶段结束跑对拍与 `npm test`**：前端改动不得破坏既有 117 条测试。
4. **未移植模块明确标注**：P3–P5 中依赖未移植 MFC 模块的部分，接口可先返回可用结果，但必须在代码与本文档标注 `[需补移植]`。

---

## 7. 实施状态（2026-09-01 首轮）

### 7.1 后端（`server/server.cpp`，全部端点已实现并冒烟通过）

| 阶段 | 端点 | 实测结果（`2A_DA_111017_115542.dat`） |
|---|---|---|
| P0 | `/api/session/open` | record_count=40, 13 波段 ✅ |
| P0 | `/api/image`（raw/2byte/intermediate） | Img1 186×88 u8，min/max 正常 ✅ |
| P0 | `/api/small-image` | SMALL_SIZE 读取 ✅ |
| P1 | `/api/analyze-path` / `/api/analyze` | s2[33] + etc[15] 真实值 ✅ |
| P2 | `/api/imageops`（gradient/niti/smooth/intermediate/to2byte） | smooth 后 u16le 43648 字符 ✅ |
| P3 | `/api/graph/make`（max_areas 默认 8）/ `combine` / `save` / `load` | 区域统计 avg/std/min/max 真实 ✅ |
| P4 | `/api/zfile/parse` | 1880 区域（ZAR 坐标文件）✅ |
| P4 | `/api/atb/load`、`/api/vtb/load` | 返回空列表 + `[需补移植]` 标注 |
| P5 | `/api/export/csv`、`/api/config/save`、`/api/config/load` | CSV 2 行写入验证 ✅ |
| 缓存 | `/api/cache/stats`、`/api/cache/clear`、`/api/cache/set-capacity` | stats 返回 bytes/capacity/file_count；clear 归零；set-capacity 调 LRU 上限 ✅ |

响应统一 `application/json; charset=utf-8`，带 CORS；除法表支持 `wtable_path`（默认理论表）。

### 7.2 前端（`web/src`，P0–P3 已接线，117/117 测试通过）

| 组件 | 接线 |
|---|---|
| `api.js` | 扩展为 P0–P5 全端点封装（此前无人引用） |
| `App.jsx` | 新增会话层：`datPath/recordCount/record/viewWave/ir1Img/ir2Img/s2/etc/batchStats/graphData`；挂载自动打开默认 `.dat`；**IR2 画布取当前波段的 2byte 版本（对应 MFC `global_twoimg`，两 pane 同 ImgX 仅位深不同，随通道切波段）** |
| `ImagePane` | 真实图像 canvas 渲染（u8 直显 / u16le 按 min-max 归一化）+ 真实灰度直方图；无数据时保持占位 |
| `DataSelectorBar` | `<`=−1 / `<(B)`=−10 / `>(N)`=+10 / `>`=+1 / `Go`=打开并跳转（对应 IDC_BACK/NEXT 系列） |
| `ChannelTab` / `SubToolbarRow` | 22 个 Img 槽位全映射（Img1–6/16–22 raw，Img7–15 intermediate），切换即换波段 |
| `ParamPanelGroup` | Sobel=gradient(gtype 0/1/2)、Binary 滑块=niti、Noise Start=smooth、Restore Image=重载（防抖 250ms） |
| `OperationPanel` | `Ope. (Start)` → 执行 ALL32 分析；Cont. Confirm OK 亦触发 |
| `StatisticsRow` | 批量 analyze（≤40 枚）→ 每维 avg/std |
| `MakeGraphRow` + `GraphPlot` | Make Graph → 真实序列/统计渲染，无数据时静态回退 |
| `S2Chart` | 显示真实 s2（或批量均值） |

通道↔波段映射为依据 `ImgType[]` 与 `WAVE_TO_IMG` 的结构推断，**需对拍确认**。

### 7.3 遗留（按阻塞项排序）

1. `[需补移植]` **CreateGraph/OnDrawPaint/CGR_CLASS** → P3 图表语义对齐（当前为区域列剖面近似）。
2. `[需补移植]` **CTemplateVTB / ATB 逻辑** → P4（当前端点返回空列表）。
3. `[需补移植]` **save_load.cpp / singan2.si2** → P5 原版格式（当前 CSV/JSON 通用格式）。
4. Validation Result 判定值依赖 ATB/VTB 模板比较，移植前保持占位默认值。
5. ~~IR2 画布暂用同面第 2 波段近似（另一面 SRU_Side 提取未移植）。~~ **已解决（2026-09-01 第二轮）**：参照 MFC `MainRun.cpp` 第 847–906 行，IR1/IR2 两画布显示同一 `ImgX`，仅位深不同（IR1=`global_oneimg` 8bit，IR2=`global_twoimg` 16bit）。Web 改为 IR2 取当前波段的 `mode=2byte`（服务端 `/api/image` 在 2byte 缺失时自动补算中间波段 Img7–15），实现"随通道切波段"。

### 7.4 第二轮（2026-09-01）：缓存管理 + IR2 跟随波段

- **根因回溯**：首轮 IR2 硬编码 `Img2`，与 MFC 两 pane 同波段的语义不符。
- **mariner_reader LRU 缓存**：`load_file_cached` 由首轮的 `std::map+mutex`（仅返回引用、无上限）升级为带容量上限的 LRU（默认 2048MB，环境变量 `SINGAN2_FILE_CACHE_MB` 覆盖）；新增 `file_cache_bytes/capacity/file_count/clear/set_capacity` 管理函数。
- **后端**：`/api/image` 的 `2byte` 模式在 `twoimg` 缺失时自动 `compute_intermediate_waves` 后重试（支持中间波段 16bit 显示）；新增 `/api/cache/stats`、`/api/cache/clear`、`/api/cache/set-capacity`。
- **前端**：`App.loadImages` 中 IR2 由 `getImage(mode:'2byte', wave:viewWave.name)` 取得，随通道切换。
- **验证**：编译通过；`test_parse` PASS（extract_mm1_side 213096 字节 / parse_zfile 1880 区域）、`test_algo` M2 对拍 **PASS 18/18**；端到端：2byte Img7/Img1 均返回 u16le，加载后缓存 `bytes=22005800 / file_count=1`，clear 后归零；`npm test` **117/117** 通过。
